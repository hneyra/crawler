name: CI Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options: [staging, production]

jobs:
  build:
    name: Build & Package
    runs-on: ubuntu-latest

    outputs:
      image-tag: ${{ steps.meta.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build JAR
        run: ./gradlew bootJar --no-daemon

      - name: Generate image metadata
        id: meta
        run: |
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "version=$SHORT_SHA" >> "$GITHUB_OUTPUT"

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: crawler-jar-${{ steps.meta.outputs.version }}
          path: build/libs/*.jar
          retention-days: 14

  # ─────────────────────────────────────────────────────────────────────────────
  # JENKINS DEPLOY — Uncomment and configure when Jenkins is ready
  # ─────────────────────────────────────────────────────────────────────────────
  #
  # deploy-jenkins:
  #   name: Trigger Jenkins Deploy
  #   runs-on: ubuntu-latest
  #   needs: build
  #   environment: ${{ github.event.inputs.environment || 'staging' }}
  #
  #   steps:
  #     - name: Trigger Jenkins pipeline
  #       run: |
  #         # Requires secrets:
  #         #   JENKINS_URL      — e.g. https://jenkins.example.com
  #         #   JENKINS_JOB_NAME — e.g. crawler/deploy
  #         #   JENKINS_USER     — Jenkins API user
  #         #   JENKINS_API_TOKEN — Jenkins API token
  #         #
  #         RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
  #           --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_API_TOKEN }}" \
  #           --data-urlencode "json={
  #             \"parameter\": [
  #               {\"name\": \"GIT_SHA\",     \"value\": \"${{ github.sha }}\"},
  #               {\"name\": \"IMAGE_TAG\",   \"value\": \"${{ needs.build.outputs.image-tag }}\"},
  #               {\"name\": \"ENVIRONMENT\", \"value\": \"${{ github.event.inputs.environment || 'staging' }}\"},
  #               {\"name\": \"BRANCH\",      \"value\": \"${{ github.ref_name }}\"}
  #             ]
  #           }" \
  #           "${{ secrets.JENKINS_URL }}/job/${{ secrets.JENKINS_JOB_NAME }}/buildWithParameters")
  #
  #         echo "Jenkins response code: $RESPONSE"
  #         if [ "$RESPONSE" != "201" ]; then
  #           echo "❌ Failed to trigger Jenkins job (HTTP $RESPONSE)"
  #           exit 1
  #         fi
  #         echo "✅ Jenkins build triggered successfully."
  #
  #     - name: Wait for Jenkins build result (optional polling)
  #       run: |
  #         # Poll Jenkins queue / build status if you want the GH Action to
  #         # reflect the Jenkins outcome. Requires the Jenkins API to be reachable
  #         # from GitHub-hosted runners (public URL or self-hosted runner on VPN).
  #         #
  #         # Suggested approach: use the official Jenkins plugin
  #         # "GitHub Pull Request Builder" or a shared library that calls back
  #         # to the GitHub Checks API instead of polling here.
  #         echo "Polling not implemented yet — configure when Jenkins is reachable."
  # ─────────────────────────────────────────────────────────────────────────────
